import _chalk from 'chalk';

// Colors
export const brand = _chalk.hex('#ff4d00');
export const { yellow } = _chalk;
export const { greenBright } = _chalk;
export const { green } = _chalk;
export const { redBright } = _chalk;
export const { blueBright } = _chalk;
export const { cyan } = _chalk;
export const { white } = _chalk;

export const { reset } = _chalk;

// Styles
export const { bold } = _chalk;
export const { dim } = _chalk;

// Whole package

export { default as chalk } from 'chalk';
