{
  "$schema": "https://json.schemastore.org/mocharc",
  "require": [
    "test/helpers/init.js",
    "ts-node/register",
    "tsconfig-paths/register"
  ],
  "spec": "test/**/*.test.ts",
  "watch-files": [
    "./src/**/*.ts",
    "./tests/**/*.test.ts"
  ],
  "forbid-only": true,
  "recursive": true,
  "reporter": "spec",
  "timeout": 60000
}
